## 1. Migration 兼容实现

- [x] 1.1 将 `V1.9.1_2__ga_ddl.sql` 精确恢复到旧官方基线内容，并用 Git blob 对比及 Flyway checksum 验证一致。
- [x] 1.2 新增唯一高序号补偿 Migration，仅在目标索引缺失时创建，并在同名索引定义异常时失败；用隔离 MySQL 覆盖缺失、正确存在和结构冲突三种情况。

## 2. 治理门禁

- [x] 2.1 新增精确摘要的 Migration 兼容例外清单并登记官方补丁，验证只有指定路径和前后摘要组合可通过。
- [x] 2.2 扩展治理回归测试，覆盖未登记历史改写、基线摘要漂移、恢复内容漂移、重复版本及合法补偿版本，并运行 `scripts/governance/test_checks.py`。

## 3. 升级预检与文档

- [x] 3.1 提供只读数据库预检，区分旧 checksum、候选 checksum、未知 checksum、失败 Migration 和索引结构，并用隔离数据库验证各退出路径。
- [x] 3.2 更新升级与验证文档，明确备份、停止条件、旧库标准路径、候选库专项规范化边界及回退策略，并验证文档不含真实凭据或环境地址。

## 4. 双路径验收

- [x] 4.1 从旧基线 Migration 初始化隔离 MySQL 后升级到修复版本，验证 validate/migrate 成功、旧 checksum 保持、补偿版本成功且索引唯一正确。
- [x] 4.2 从空隔离 MySQL 执行修复版本全量迁移，验证 Migration 数量、checksum 和最终索引结构与升级路径一致。
- [x] 4.3 使用修复版本应用连接升级后的隔离数据库并完成启动检查，运行 OpenSpec 严格校验和受影响 CI 检查，将可复现证据写入验证文档。
